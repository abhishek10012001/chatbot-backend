/**
 * Constants that represent a collection in our database.
 * If collection name starts with a capital letter, it is a root-level collection.
 */
export enum DBCollection {
    /** Top-level "Messages" collection */
    Messages = "Messages",

}  